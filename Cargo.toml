[package]
name = "bevy_sandbox"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.0", features = ["dynamic_linking"] }
bevy-inspector-egui = "0.36.0"
bevy_scene = "0.18.0"

[profile.dev]
opt-level = 1
